query GetHeaderMenu($id:ID!) {
    menu(id: $id, idType: DATABASE_ID) {
        menuItems {
            edges {
                node {
                    label
                    path
                }
            }
        }
    }
}